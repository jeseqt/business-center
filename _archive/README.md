# Archive

This directory contains archived files that are no longer in active use but kept for reference.

- `database/`: Old database migration scripts. Please use `supabase/migrations/` for the latest database schema.
